{
  "name": "agrovision-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "deploy": "vercel",
    "deploy:prod": "vercel --prod"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "react-chartjs-2": "^5.3.1"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
